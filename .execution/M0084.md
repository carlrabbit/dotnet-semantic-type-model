# M0084 Execution Ledger — PR 127 Hardening

## State

- Milestone: M0084 Coordinated TestData Generation (re-opened hardening pass)
- PR: 127
- Branch: `feat/m0084-coordinated-testdata`
- Baseline head: `cf973a9ed087f444b41d591dcd8b1e1fb30e0db7`
- Base: `main`
- Milestone version: `6.1.0-m0084`
- Current phase: implementation hardening complete locally; branch integration and aggregate validation pending
- Planning decisions required: none identified

This ledger is operational state only. The re-opened milestone and its authority documents control.

## Coverage and work packages

### WP1 — Profile coordination composition and validation

- Preserve coordinated fields with left-to-right field-level overlay instead of replacing a whole coordination record.
- Revalidate effective composed coordination fields, contradictory combinations, scopes, target eligibility, dependencies, and cycles after composition.
- Validate dependency graphs across all profile layers and effective object/property semantics.
- Cover supported `Derived + Unique` and `Sequence + Unique`, rejected `Derived + Shared`, `Sequence + Shared`, `Shared + Unique`, and `Derived + Sequence`.
- Evidence gate: focused profile composition/validation tests, including composition-only cycles and contradictions.

### WP2 — Effective inherited dependency semantics

- Make profile validation and runtime object planning use the same effective inherited/composed property set.
- Allow valid derived rules to read inherited scalar/enum siblings.
- Reject dependencies that exist only on a more-derived containing type when the target declaring effective object does not contain them.
- Preserve stable unrelated-property order and dependency-before-dependent execution.
- Evidence gate: focused inheritance, declaration-order, chain, cycle, self, unknown, cross-object, and non-scalar dependency tests.

### WP3 — Coordinated scalar and enum source pipeline

- Route scalar and enum properties through the documented precedence pipeline.
- Validate profile weighted enum candidates against declared enum values and select only legal candidates.
- Support derived and sequence enum results through canonical candidate validation.
- Preserve higher-precedence property/Logical-Type programmatic generators for enum properties.
- Ensure invalid coordinated candidates fail closed without fallback.
- Evidence gate: focused enum/profile/programmatic/invalid-source tests and retained M0082 enum regression tests.

### WP4 — Coordination diagnostics and dependency-context semantics

- Map derived/sequence callback exceptions to `TESTDATA_COORDINATION_CALLBACK_FAILED`.
- Map null or canonically invalid coordinated outputs to `TESTDATA_COORDINATION_CANDIDATE_INVALID`.
- Map unavailable dependencies to `TESTDATA_COORDINATION_DEPENDENCY_UNAVAILABLE` and undeclared reads to `TESTDATA_COORDINATION_DEPENDENCY_UNDECLARED`.
- Make incompatible `TryGet<T>` requests programmer/configuration errors; retain false only for omitted/explicit-null declared dependencies.
- Prevent coordinated failures from falling through to lower sources.
- Evidence gate: focused diagnostic and context behavior tests.

### WP5 — Stateful rule semantics and session isolation

- Verify/fix Root/Batch sequence, shared, uniqueness, retry, collision, binary equality, omission/null participation, and shadowing behavior.
- Ensure explicit programmatic/derived/sequence duplicates fail without hidden reinvocation.
- Preserve failed-generation atomicity and fresh state after failure, repeated invocation, and concurrent invocation.
- Evidence gate: focused sequence/shared/unique/session/failure/concurrency tests; retained M0082/M0083 regressions.

### WP6 — Consumer sample and documentation/diagnostics

- Harden `coordinated-generation` packed-package catalog scenario to expose required Scope H behavior and assertions.
- Synchronize README, TestData guide, samples index, shared NuGet README, compatibility, diagnostics, and 6.1.0 release notes as applicable.
- Keep documentation explicit that coordination is TestData policy, not canonical keys, relationships, or persistent datasets.
- Evidence gate: `eng/public-docs.ps1` and packed sample execution.

### WP7 — Validation and branch integration

- Run focused Tier 1 validation on the hardened implementation.
- Update branch against current `main` using normal Git workflow and resolve conflicts without discarding authority/current-main changes.
- Run Tier 2 repository check.
- Run Tier 3 package, package-smoke, samples, public-docs, and aggregate `release-check 6.1.0-m0084` on final merge-candidate head.
- Verify PR 127 remains associated with this branch, points at current hardened head, is mergeable, and its `ci/check` succeeds on that head.
- No publish, tag, GitHub Release, stable `release-check 6.1.0`, or publication-channel release work.

## Acceptance matrix coverage

The original M0084 acceptance criteria are covered by WP1–WP7 as follows:

- Session lifecycle/isolation and failure atomicity: WP5 and WP7.
- Public profile capability, immutability, scopes, composition, and M0083 compatibility: WP1, WP5.
- Derived values, dependency planning, context semantics, canonical validation, and fail-closed behavior: WP1–WP4.
- Sequences: WP5.
- Shared values: WP5.
- Scoped uniqueness: WP5.
- Rule combinations and source precedence: WP1, WP3, WP5.
- Determinism and M0082/M0083 regression compatibility: WP2, WP3, WP5, WP7.
- Package-consumer sample: WP6, WP7.
- Documentation and diagnostics: WP4, WP6, WP7.
- Architecture, compatibility, public API, non-goals, and aligned package inventory: WP1–WP7 review gate.

The PR 127 Hardening Acceptance Matrix is explicitly represented by WP1–WP7, with enum source resolution in WP3, composition/revalidation in WP1, inheritance/planning in WP2, dependency diagnostics in WP4, sequence/shared/uniqueness in WP5, session/failure/regression in WP5, and integration readiness in WP7.

## Evidence log

| Work package | Status | Evidence |
|---|---|---|
| WP1 | complete locally | Coordination overlays now merge fields; `Compose` rebuilds through profile validation; focused tests cover `Derived + Unique`, contradictions, and composition-only cycles. |
| WP2 | complete locally | Profile validation includes inherited effective properties and focused inherited dependency/planning tests pass. |
| WP3 | complete locally | Scalar/enum coordinated/profile/programmatic source routing and canonical enum validation corrected; focused enum tests pass. |
| WP4 | complete locally | Callback, candidate, unavailable, undeclared, and incompatible-context behavior has dedicated diagnostics/tests. |
| WP5 | complete locally | Binary-content uniqueness, finite-domain exhaustion, omission/index behavior, and repeated invocation isolation tests pass; full state matrix still requires final aggregate validation. |
| WP6 | pending | Existing scenario/docs require final packed-consumer review. |
| WP7 | pending | Baseline PR head was mergeable; rebase/current-main and final CI evidence required after edits. |

## Local focused evidence

- `eng/test-project.ps1 tests/unit/SemanticTypeModel.TestData.Tests.Unit/SemanticTypeModel.TestData.Tests.Unit.csproj` — passed, 52/52.
- `git diff --check` — passed.

## Resume point

Implement WP1–WP4 first, then add focused tests for each corrected contract before stateful/session and documentation review. Do not mark any package complete from source inspection alone.

## Completion cleanup

After fresh milestone reread, reconciliation, completion audit, final aggregate release-check, and PR/CI evidence, remove this ledger and the completed milestone file according to repository completed-milestone cleanup. Durable M0084 outcomes remain in current authority documents.
