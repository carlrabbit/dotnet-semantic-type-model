# Sample - Power BI Projection

Project: `samples/powerbi-projection`

Demonstrates:

- projection from canonical runtime model to TOM-like metadata;
- no Power BI service connection.

Run:

```sh
dotnet run --project samples/powerbi-projection/powerbi-projection.csproj
```
