# Sample - JSON Schema Roundtrip

Project: `samples/json-schema-roundtrip`

Demonstrates:

- JSON Schema Draft 2020-12 import;
- canonical runtime model adaptation and transformation pipeline;
- JSON Schema export through runtime projection.

Run:

```sh
dotnet run --project samples/json-schema-roundtrip/json-schema-roundtrip.csproj
```
