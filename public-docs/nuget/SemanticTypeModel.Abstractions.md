# SemanticTypeModel.Abstractions

`SemanticTypeModel.Abstractions` provides canonical contracts shared by all SemanticTypeModel packages.

```sh
dotnet add package SemanticTypeModel.Abstractions --version 2.2.0
```

This package is part of the stable package set. Public APIs and package boundaries follow the compatibility policy.
