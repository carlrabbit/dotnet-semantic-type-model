# SemanticTypeModel.Abstractions

`SemanticTypeModel.Abstractions` provides canonical contracts shared by all SemanticTypeModel packages.

```sh
dotnet add package SemanticTypeModel.Abstractions --version 1.0.0
```

This package is part of the stable 1.0 package set. Public APIs follow the compatibility policy.
