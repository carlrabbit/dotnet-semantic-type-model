# SemanticTypeModel.Core

`SemanticTypeModel.Core` provides model building and transformation pipeline components.

```sh
dotnet add package SemanticTypeModel.Core --version 0.1.0-alpha
```

This is an initial prerelease. APIs may change before 1.0.
