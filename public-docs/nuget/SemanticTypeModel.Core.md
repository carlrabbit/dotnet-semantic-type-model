# SemanticTypeModel.Core

`SemanticTypeModel.Core` provides model building and transformation pipeline components.

```sh
dotnet add package SemanticTypeModel.Core --version 1.1.0
```

This package is part of the stable package set. Public APIs and package boundaries follow the compatibility policy.
