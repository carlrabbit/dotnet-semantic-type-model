# SemanticTypeModel.Generators

`SemanticTypeModel.Generators` contains the incremental source generator for semantic model provider generation.

```sh
dotnet add package SemanticTypeModel.Generators --version 0.1.0-alpha
```

This is an initial prerelease. Generator output shape may change before 1.0.
