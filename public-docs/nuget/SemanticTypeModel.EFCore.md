# SemanticTypeModel.EFCore

`SemanticTypeModel.EFCore` projects canonical models into EF Core-oriented model definitions.

```sh
dotnet add package SemanticTypeModel.EFCore --version 0.1.0-alpha
```

This is an initial prerelease. APIs may change before 1.0.
